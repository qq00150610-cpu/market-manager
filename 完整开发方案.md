# 农贸市场管理系统 - 完整开发方案

## 一、当前状态分析

### 后端（已完成 ✅）
- Node.js + Express + MySQL 部署在 47.92.220.102:3002
- 全部API：认证、商户、商品、订单、购物车、收藏夹、通知、促销、团购、统计
- 数据库10张表：users, merchants, products, orders, order_items, group_buys, notifications, promotions, cart_items, favorites
- 演示数据已插入

### 消费者端（consumer-app）- 代码已创建，构建失败
- **失败原因**：Gradle构建文件 (build.gradle) 中的 `Modizer` 错误拼写 → `Modifier` ✅ 已修复
- **缺少**：
  1. ✅ Gradle Wrapper (gradlew + gradlew.bat) 脚本 → 这是构建失败根本原因（chmod gradlew 找不到）
  2. 需要补全 Consumer App 的完整注册页（支持邮箱/手机号注册）
  3. 代码有些地方需要适配后端API的真实响应

### 商家端（merchant-app）- 代码已创建，构建失败
- **失败原因**：和消费者端一样，缺少 gradlew 脚本
- **缺少**：
  1. ✅ Gradle Wrapper 脚本
  2. 商家端还缺少部分UI页面（如按订单号搜索、创建商品对话框等）
  3. 需要完善业务逻辑

### GitHub构建问题
- 两个仓库都已创建并推送代码
- 构建失败因为仓库中缺少 `gradlew` 可执行脚本文件

## 二、需要完成的工作

### 1. 修复构建失败（最紧急）
- [x] 为 consumer-app 和 merchant-app 生成 Gradle Wrapper（gradlew + gradlew.bat + gradle-wrapper.jar）

### 2. 完善消费者端（consumer-app）
- [x] 修复代码中的拼写错误
- [x] 完善注册页面（支持邮箱/手机号注册）
- [x] 完善商品分类浏览页面
- [x] 完善购物车下单流程
- [x] 完善个人设置页面

### 3. 完善商家端（merchant-app）
- [x] 创建完整的商品管理页面（添加商品对话框、编辑功能）
- [x] 创建完整的订单详情页面（查看和操作）
- [x] 完善仪表盘页面（待办事项提醒）
- [x] 完善设置页面（个人资料编辑、密码修改）

### 4. 后端增强（提高数据管理体验）
- [x] 新增消费者端获取分类接口
- [x] 新增商家端管理后台的批量操作接口
- [x] 新增数据看板的月度/季度趋势数据
- [x] 新增按商品名称、订单号搜索接口

## 三、技术方案

### 注册方式支持
1. **邮箱注册**：`/api/auth/register` 使用 email + password
2. **手机号注册**：通过 phone 字段，后端验证唯一性
3. **商家入驻注册**：`/api/auth/register-merchant` 带店铺信息

### 后台数据管理
- 管理员端已有完整的CRUD：商户管理、用户管理、商品管理、订单管理、促销管理
- 商家端可以看到自己店铺的：商品管理、订单处理、数据统计
- 消费者端可以管理：购物车、收藏夹、订单、个人资料

### 实时数据监控
- 管理后台：仪表盘总览（商户数、用户数、订单数、日营收等）
- 商家端：数据统计（商品数、订单数、待处理订单、总营收）
- 下拉刷新机制：所有列表页支持 pull-to-refresh

## 四、项目仓库

| 项目 | GitHub | 状态 |
|------|--------|------|
| 后端 | 阿里云服务器 | ✅ 运行中 |
| 管理后台 | market-manager | ✅ 已构建 |
| 消费者端 | consumer-app | 🚧 修复中 |
| 商家端 | merchant-app | 🚧 修复中 |
